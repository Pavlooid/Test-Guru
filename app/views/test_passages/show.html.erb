<h1>Прохождение теста <%= @test_passage.test.title %></h1>

<p><strong> 
  Общее количество вопросов - <%= @test_passage.all_questions_count %><br>
  Номер текущего вопроса - <%= @test_passage.current_question_number %>
</strong></p>

<p>
  Текущий вопрос - <strong><%= @test_passage.current_question.question %></strong>
</p>

<p>Варианты ответов:</p>


<%= form_with url: test_passage_path(@test_passage), local: true, method: :put do |form| %>
  <%= form.collection_check_boxes :answer_ids, @test_passage.current_question.answers, :id,
    :body, include_hidden: false do |b| %>
    <p>
      <%= b.check_box id: "answer_ids_#{b.object.id}" %>
      <%= b.label %>
    </p> 
  <% end %>
  <p><%= form.submit 'Next question' %></p>
<% end %> 
