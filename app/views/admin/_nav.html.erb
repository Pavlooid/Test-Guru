<nav class="navbar-text navbar-light bg-light">
  <span>
    <%= link_to t('general.guru'), about_path %> 
  </span>

  <span class="navbar-text position-absolute top-0 end-0">
    <%= t('general.welcome_admin') %> <b class="nav-admin user-email"><%= current_user.email %></b> <%= t('general.guru') %> |
    <%= link_to t('general.tests'), tests_path if current_user.is_a?(Admin) %> |
    <%= link_to t('general.exit'), destroy_user_session_path, method: :delete %>
  </span>
</nav>

