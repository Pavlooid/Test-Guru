<%= form_with model: [:admin, @test, @question], local: true do |form| %> 
<%= render 'shared/errors', resource: @question %>

  <p>
    <%= form.label :title, t('.title') %><br>
    <%= form.text_field :title %>
  </p><br>

  <p>
    <%= form.label :question, t('.question') %><br>
    <%= form.text_field :question%>
  </p><br>

  <p><%= form.submit %></p>
  <% end %>
