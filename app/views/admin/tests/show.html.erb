<h1>Тема теста - "<%= @test.title %>"</h1>

<p>
  <%= link_to 'Добавить вопрос к этому тесту', new_admin_test_question_path(@test) %> |
  <%= link_to 'Редактировать этот тест', edit_admin_test_path(@test) %> |
  <%= link_to 'Все тесты', admin_tests_path %>
</p>

<p><strong>Категория - </strong><%= @test.category.title %></p> 
<p><strong>Сложность - </strong><%= @test.level %></p>
<p><strong>Автор - </strong><%= @test.author.email %></p>

<p><strong>Вопросы теста:</strong></p>
<% if @test.questions.empty? %>
  <p>У данного теста нет привязанных вопросов</p>
<% else %>
<table>
  <tr>
    <th>ID</th>
    <th>Тема</th>
    <th>Вопрос</th>
    <th colspan="3">Действия</th>
  </tr>
  <% @test.questions.each do |question| %>
    <tr>
      <td><%= question.id %></td>
      <td><%= question.title %></td>
      <td><%= question.question %></td>
      <td><%= link_to 'К вопросу', admin_question_path(question) %></td>
      <td><%= link_to 'Изменить', edit_admin_question_path(question) %></td>
      <td>
        <%= link_to 'Удалить',
                    admin_question_path(question),
                    method: :delete,
                    data:   { confirm: 'Вы уверены?' } %>
    </tr>
  <% end %>
</table>
<% end %>
