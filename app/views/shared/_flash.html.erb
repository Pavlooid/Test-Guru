<% flash.each do |key, message| %>
  <div class="alert alert-<%= key %>"role="alert" > <%= message.html_safe %>
    <button type="button" class="btn-close" aria-label="Close"></button>
    </div>
<% end %>
